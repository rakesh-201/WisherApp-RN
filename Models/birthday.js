export default class Birthday {
    constructor(
        name,
        number,
        date,
        message,
        image,
        identifier
    ) {
        this.name = name
        this.number = number
        this.date = date
        this.message = message
        this.image = image
        this.identifier = identifier
    }
}